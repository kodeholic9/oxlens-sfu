[workspace]
# ★재작성 중(spec/v1.1) — 덩어리마다 아래층부터 members 를 되돌린다.
#   그래야 `cargo build`(workspace 전량)가 게이트로 살아 있다 — `-p` 스코프는 쓰지 않는다.
#   복구 순서: oxsig → common → oxhubd → oxsfud.
members = ["crates/oxsig"]
resolver = "3"

[workspace.package]
edition = "2024"
license = "Apache-2.0"
authors = ["Tae Goo Kang <kodeholic99@gmail.com>"]

[workspace.dependencies]
oxsig = { path = "crates/oxsig" }
common = { path = "crates/common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
jsonwebtoken = "9"
chrono = "0.4"
tokio = { version = "1", features = ["full"] }
nix = { version = "0.26", features = ["signal"] }
axum = { version = "0.7", features = ["ws"] }
tower-http = { version = "0.5", features = ["cors"] }
futures-util = "0.3"
dashmap = "6"
arc-swap = "1"
uuid = { version = "1", features = ["v4"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
async-trait = "0.1"
tonic = "0.12"
prost = "0.13"
tonic-build = "0.12"
tokio-stream = { version = "0.1", features = ["sync"] }
getrandom = { version = "0.3", features = ["std"] }
sha2 = "0.10"
dtls = "0.17.1"
webrtc-srtp = "0.17.1"
webrtc-util = "0.17.1"
sctp-proto = "0.9"
tokio-util = { version = "0.7", features = ["rt"] }
bytes = "1"
hmac = "0.12"
sha1 = "0.10"
crc32fast = "1"

[workspace.lints.rust]
unsafe_code = "forbid"
unused_must_use = "deny"

[workspace.lints.clippy]
all = { level = "deny", priority = -1 }
